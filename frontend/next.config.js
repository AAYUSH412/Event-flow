/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  images: {
    remotePatterns: [
      {
        protocol: 'https',
        hostname: '**',
      },
      {
        protocol: 'http',
        hostname: '**',
      },
    ],
  },
  // For handling static assets correctly
  poweredByHeader: false,
  reactStrictMode: true,
};

module.exports = nextConfig;